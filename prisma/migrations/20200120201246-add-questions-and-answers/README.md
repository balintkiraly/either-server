# Migration `20200120201246-add-questions-and-answers`

This migration has been generated by kiralybalint at 1/20/2020, 8:12:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "either2"."Answer" (
  "answer" text NOT NULL DEFAULT 'optionA' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "either2"."User" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "email" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "name" text   ,
  "password" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "either2"."Question" (
  "content" text NOT NULL DEFAULT '' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "optionA" text NOT NULL DEFAULT '' ,
  "optionB" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "either2"."Answer" ADD COLUMN "question" text NOT NULL  REFERENCES "either2"."Question"("id") ON DELETE RESTRICT,
ADD COLUMN "user" text NOT NULL  REFERENCES "either2"."User"("id") ON DELETE RESTRICT;

ALTER TABLE "either2"."Question" ADD COLUMN "author" text NOT NULL  REFERENCES "either2"."User"("id") ON DELETE RESTRICT;

CREATE UNIQUE INDEX "User.email" ON "either2"."User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20200120201246-add-questions-and-answers
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,41 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = env("POSTGRESQL_URL")
+}
+
+enum AnswerEnum {
+  optionA
+  optionB
+}
+
+model Answer {
+  id        String     @default(cuid()) @id
+  answer    AnswerEnum
+  user      User       @relation(name: "UserAnswers")
+  question  Question   @relation(name: "QuestionAnswers")
+  createdAt DateTime   @default(now())
+}
+
+model User {
+  id        String     @default(cuid()) @id
+  email     String     @unique
+  password  String
+  name      String?
+  questions Question[] @relation(name: "UserQuestion")
+  answers   Answer[]   @relation(name: "UserAnswers")
+  createdAt DateTime   @default(now())
+}
+
+model Question {
+  id        String   @default(cuid()) @id
+  content   String
+  optionA   String
+  optionB   String
+  author    User     @relation(name: "UserQuestion")
+  answers   Answer[] @relation(name: "QuestionAnswers")
+  createdAt DateTime @default(now())
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20200120201246-add-questions-and-answers)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20200120201246-add-questions-and-answers'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
